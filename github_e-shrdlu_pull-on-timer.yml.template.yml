on:
  schedule:
    - cron: '0 * * * *' # every hour
    # - cron: '*/15 * * * *' # every 15mins
    # - cron: '0 2 * * *' # every day @ 2am
  workflow_dispatch:

jobs:
  sync:
    uses: e-shrdlu/workflows/.forgejo/workflows/github_e-shrdlu_pull-on-timer.yml@main
    secrets: inherit
